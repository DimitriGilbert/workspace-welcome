# Attic

Quarantined 2026-09-07 during the widget-normalization consistency pass (PC.1): these are
committed-by-mistake artifacts that were cluttering the working surface — a fleet-initiative
test-alignment plan (`TEST-ALIGNMENT-PLAN.md`, 2026-09-05, produced read-only against a
since-superseded branch), a desktop screenshot (`Screenshot_20260906_172900.jpeg`), and a
temporary Vite-SSR probe script for the grid-layout compaction chain
(`compact-chain.tmp.mjs`, formerly `apps/web/compact-chain.tmp.mjs`). Per the owner's order
("keep the script in case they are needed, but the mess is appalling") they were relocated
here with `git mv` — renamed, never deleted — so the repo root and `apps/web/` stay clean
while every byte remains recoverable from this directory.
